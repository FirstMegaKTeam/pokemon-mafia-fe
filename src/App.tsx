import React from 'react';
import { Example } from 'types';

import './App.css';

export const App = () => {
    const test: Example = {
        example: 'example',
        test: 2,
    };

    return (
        <h1>test</h1>
    );
};
